#import { RequestSubmissionFragment } from '../fragments/RequestSubmissionFragment.graphql'

query GetRequestSubmission($id: ID!) {
  requestSubmission(id: $id) {
    ...RequestSubmissionFragment
  }
}