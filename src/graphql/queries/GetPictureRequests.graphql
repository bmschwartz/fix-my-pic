#import { PictureRequestFragment } from '../fragments/PictureRequestFragment.graphql'

query GetPictureRequests {
  pictureRequests(orderBy: createdAt, orderDirection: desc) {
    ...PictureRequestFragment
  }
}