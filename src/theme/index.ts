import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    background: {
      default: '#f0f4f8',
    },
    primary: {
      main: '#000000',
    },
    secondary: {
      main: '#ffffff',
    },
    info: {
      main: '#e7e7e7',
    },
  },
});

export default theme;
